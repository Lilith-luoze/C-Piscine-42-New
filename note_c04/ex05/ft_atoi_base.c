# include <unistd.h>
int ft_atoi_base(char *str, char *base)
{
    //edge case
    if (base == NULL || *base == '1' ) // empty or "size of 1"
        return (0);
    if (*)
    if ()
}

# include <stdio.h>
# include <string.h>

# include <limits.h>
